tgstickers
==========

* `docker compose up`
* `python3 -m venv env`
* `source env/bin/activate`
* `pip install -r requirements.txt`
* `python migration.py`
* create env with name `API_TOKEN`
* `python telegram.py`